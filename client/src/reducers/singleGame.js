const initialState = {
  example: 0
};

const singleGameReducer = (initialState, action) => {
  switch (action.type) {
    case 'UPDATE GAME':
      return state;
    case 'UPDATE ROUND':
      return state;
    default: 
      return state;
  }
};

export default singleGameReducer;